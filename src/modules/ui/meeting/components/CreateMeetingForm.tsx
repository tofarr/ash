import React, { FC } from 'react';

const CreateMeetingForm: FC = () => {
  return <div>
    TODO: A form to create a meeting.
    Should have a form field for each box.
    Creates a separate transaction for each box.
    Boxes are defined in settings.
    Should auto calculate totals
    Has values for cash and cheques, auto calculate one if other specified
  </div>
}
